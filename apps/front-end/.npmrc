# This machine's global ~/.npmrc defaults to a work/employer AWS CodeArtifact
# registry, which doesn't mirror public packages (clsx, tailwind-merge, etc.).
# Force the public registry for this project regardless of that global default.
registry=https://registry.npmjs.org/
@ntrs:registry=https://registry.npmjs.org/
